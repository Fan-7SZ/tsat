# OAuth proxy worker base URL (Cloudflare Worker, see OuthWorker).
# Build-time injected by Vite; set in Cloudflare Pages env vars for deploys,
# or in .env.local for local development. Baked into the client bundle.
VITE_AUTH_WORKER_URL=https://your-auth-worker.example.workers.dev
